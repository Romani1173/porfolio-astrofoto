---
import { getCollection } from 'astro:content';
import CatalogPage from '../../components/CatalogPage.astro';
const fotos = (await getCollection('fotos')).sort((a, b) => b.data.entorn.data.valueOf() - a.data.entorn.data.valueOf());
---

<CatalogPage lang="en" fotos={fotos} />
